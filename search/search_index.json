{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"MOSAIC","text":"<p>A Modular System for Adaptive Human\u2013AI Collaboration</p> <p> </p> <p>A grid-based Search and Rescue simulation platform for studying human\u2013AI teaming, built on top of MiniGrid.</p>"},{"location":"#what-is-mosaic","title":"What is MOSAIC?","text":"<p>MOSAIC is named after the art of constructing a coherent picture from individual tiles. The platform combines modular components \u2014 simulation environments, AI agents, human interfaces, multimodal sensing, and analytics \u2014 to support reproducible Human\u2013AI collaboration research. The name also reflects the grid-based structure of the underlying environments, where complex collaborative behaviors emerge from interactions within a tiled world.</p> <p>Its first testbed is a search-and-rescue scenario: a building on fire, victims trapped, some rooms locked, lava blocking your path. Your mission \u2014 human, AI, or both together \u2014 is to save everyone before time runs out. MOSAIC isn't tied to search and rescue; the same modular pieces can support other collaborative domains as the platform grows.</p>"},{"location":"#features","title":"Features","text":"Feature Description Multi-Room Layouts Navigate through configurable grid-based buildings Real vs Fake Victims Distinguish cross-shaped victims (\u271a) from T-shaped decoys (\u22a4) Lava Hazards One wrong step and it's game over Locked Rooms Find keys to unlock doors and reach trapped victims Interactive GUI Pygame interface with real-time info panels RL-Ready Gymnasium compatible for training rescue agents Lab Streaming Layer Sync with eye trackers, EEG, and other physiological sensors LLM Integration Prompt-driven agent reasoning via a pluggable LLM client"},{"location":"#where-to-go-next","title":"Where to go next","text":"<ul> <li>Getting Started \u2014 install MOSAIC and run your first mission</li> <li>Architecture \u2014 how <code>mosaic/</code> (the installable package) and <code>experiment/</code> (this lab's study wiring) fit together</li> <li>API Reference \u2014 auto-generated reference from the source docstrings</li> </ul>"},{"location":"architecture/","title":"Architecture","text":"<p><code>mosaic/</code> is the installable package (<code>pyproject.toml</code>'s <code>packages.find</code>) and ships generic, neutral defaults \u2014 no study-specific tuning. <code>experiment/</code> (excluded from packaging) holds this lab's calibrated parameters, orchestration, and entry points, wired on top of <code>mosaic/</code>'s injection points.</p> <pre><code>mosaic/\n\u251c\u2500\u2500 src/\n\u2502   \u2514\u2500\u2500 mosaic/\n\u2502       \u251c\u2500\u2500 core/\n\u2502       \u2502   \u251c\u2500\u2500 camera.py        # Camera strategy implementations\n\u2502       \u2502   \u251c\u2500\u2500 placers.py       # Placer ABC\n\u2502       \u2502   \u2514\u2500\u2500 level.py         # SARLevelGen base class\n\u2502       \u251c\u2500\u2500 sar/\n\u2502       \u2502   \u251c\u2500\u2500 env.py           # PickupVictimEnv (main environment), build_sar_env() factory\n\u2502       \u2502   \u251c\u2500\u2500 objects.py       # Victim and FakeVictim classes\n\u2502       \u2502   \u251c\u2500\u2500 actions.py       # RescueAction + RescueRewards (neutral defaults)\n\u2502       \u2502   \u251c\u2500\u2500 observations.py  # GameObservation processor\n\u2502       \u2502   \u251c\u2500\u2500 instructions.py  # PickupAllVictimsInstr mission\n\u2502       \u2502   \u2514\u2500\u2500 placers.py       # VictimPlacer (neutral health), LavaPlacer, LockedRoomPlacer\n\u2502       \u251c\u2500\u2500 gui/\n\u2502       \u2502   \u251c\u2500\u2500 main.py          # SAREnvGUI controller (constructor-injectable components)\n\u2502       \u2502   \u251c\u2500\u2500 user.py          # Keyboard input + LLM threading\n\u2502       \u2502   \u251c\u2500\u2500 info.py          # InfoPanel (stats, object table)\n\u2502       \u2502   \u251c\u2500\u2500 chat.py          # ChatPanel (LLM messages)\n\u2502       \u2502   \u251c\u2500\u2500 feedback.py      # EdgeVignette (configurable edge feedback)\n\u2502       \u2502   \u2514\u2500\u2500 theme.json       # pygame_gui UI theme\n\u2502       \u251c\u2500\u2500 llm/\n\u2502       \u2502   \u251c\u2500\u2500 client.py        # LLMClient contract (ABC) + DummyLLMClient\n\u2502       \u2502   \u251c\u2500\u2500 parser.py        # LLM response cleaning\n\u2502       \u2502   \u251c\u2500\u2500 process_prompts.py  # Prompt generation from game state\n\u2502       \u2502   \u2514\u2500\u2500 pathfinding.py   # Pathfinding queries for LLM context\n\u2502       \u2514\u2500\u2500 tutorial_env.py      # Single-room tutorial environment\n\u251c\u2500\u2500 experiment/                  # excluded from the installable package\n\u2502   \u251c\u2500\u2500 main.py                  # Entry point: interactive GUI mode (python -m experiment.main)\n\u2502   \u251c\u2500\u2500 experiment_main.py       # Entry point: full research experiment\n\u2502   \u251c\u2500\u2500 llm.py                   # build_llm_client() \u2014 provider wiring (OpenAI/Google via llama_index)\n\u2502   \u251c\u2500\u2500 replay.py                # Replay recorded sessions\n\u2502   \u251c\u2500\u2500 utils.py                 # Utility classes (ColorPrint, skip_run)\n\u2502   \u251c\u2500\u2500 placers.py               # LavaRiskVictimPlacer \u2014 this study's health/decay tuning\n\u2502   \u251c\u2500\u2500 pacing.py                # TunedPickupVictimEnv \u2014 this study's pacing formula\n\u2502   \u251c\u2500\u2500 game.py                  # SARGame task with LSL streaming; wires tuned params into mosaic\n\u2502   \u251c\u2500\u2500 tutorial.py              # SARTutorial task\n\u2502   \u2514\u2500\u2500 instructions.yaml        # Participant-facing instructions\n\u251c\u2500\u2500 configs/\n\u2502   \u251c\u2500\u2500 experiment.yaml          # Full experiment configuration\n\u2502   \u2514\u2500\u2500 config.yml               # Base configuration\n\u251c\u2500\u2500 tests/\n\u251c\u2500\u2500 docs/\n\u251c\u2500\u2500 mkdocs.yml\n\u251c\u2500\u2500 requirements.txt\n\u2514\u2500\u2500 pyproject.toml\n</code></pre>"},{"location":"architecture/#the-mosaic-experiment-split","title":"The mosaic / experiment split","text":"<p><code>mosaic/</code> defines contracts and generic defaults; <code>experiment/</code> supplies this lab's specific implementations and wires them in via constructor injection.</p> <pre><code>flowchart LR\n    subgraph mosaic[\"mosaic/ (installable, neutral)\"]\n        LLMClient[\"LLMClient (ABC)\"]\n        Dummy[\"DummyLLMClient\"]\n        Placer[\"Placer (ABC)\"]\n        VictimPlacer[\"VictimPlacer\"]\n        GUI[\"SAREnvGUI(llm_client, ...)\"]\n    end\n\n    subgraph experiment[\"experiment/ (this lab's study)\"]\n        LlamaIndexLLMClient[\"LlamaIndexLLMClient\"]\n        buildLlmClient[\"build_llm_client()\"]\n        LavaRiskVictimPlacer[\"LavaRiskVictimPlacer\"]\n    end\n\n    LLMClient -.implements.-&gt; LlamaIndexLLMClient\n    LLMClient -.implements.-&gt; Dummy\n    Placer -.implements.-&gt; VictimPlacer\n    Placer -.implements.-&gt; LavaRiskVictimPlacer\n    buildLlmClient --&gt;|constructs| LlamaIndexLLMClient\n    buildLlmClient --&gt;|injected into| GUI\n    LavaRiskVictimPlacer --&gt;|injected into| GUI</code></pre> <p>Contract lives in <code>mosaic</code>, wiring lives in <code>experiment</code></p> <ul> <li><code>mosaic.llm.client.LLMClient</code> is an abstract contract (<code>query(prompt) -&gt; str</code>); <code>mosaic.llm.client.DummyLLMClient</code>   is a no-op implementation useful for testing without API keys. The concrete provider-backed   implementation (<code>LlamaIndexLLMClient</code>, wrapping OpenAI/Google via <code>llama_index</code>) and its   <code>build_llm_client()</code> factory live in <code>experiment/llm.py</code>, not in <code>mosaic</code>.</li> <li><code>mosaic.core.placers.Placer</code> is an abstract placement contract; <code>mosaic.sar.placers.VictimPlacer</code>   ships neutral defaults, while <code>experiment.placers.LavaRiskVictimPlacer</code> carries this study's   tuned health/decay behavior.</li> <li><code>mosaic.gui.main.SAREnvGUI</code> accepts every major component (<code>llm_client</code>, <code>prompt_builder</code>,   <code>response_processor</code>, <code>user</code>, <code>info_panel</code>, <code>chat_panel</code>, <code>vignette</code>) as constructor   parameters, defaulting to the neutral <code>mosaic</code> implementations when omitted \u2014 callers (like   <code>experiment/</code>) compose it from already-built instances rather than subclassing or passing   partial factories.</li> </ul>"},{"location":"architecture/#design-patterns","title":"Design patterns","text":"<ul> <li>Strategy Pattern \u2014 Camera system (<code>core/camera.py</code>): swap strategies at runtime via <code>env.switch_camera(...)</code>.</li> <li>Dependency Injection \u2014 <code>Placer</code>, <code>LLMClient</code>, and the GUI's panels/vignette are all injected as   constructed instances rather than looked up or subclassed.</li> <li>Factory function \u2014 <code>build_sar_env()</code> constructs a fully configured <code>PickupVictimEnv</code> from   already-built placer instances.</li> <li>Template Method \u2014 <code>SARLevelGen</code> defines level generation hooks for subclasses (<code>PickupVictimEnv</code>, <code>TutorialEnv</code>).</li> </ul>"},{"location":"architecture/#high-level-data-flow","title":"High-level data flow","text":"<pre><code>flowchart TD\n    A[\"Keyboard Input\"] --&gt; B[\"User.handle_key()\"]\n    B --&gt; C[\"PickupVictimEnv.step(action)\"]\n    C --&gt; C1[\"Move / rotate agent\"]\n    C --&gt; C2[\"Trigger RescueAction (pickup)\"]\n    C --&gt; C3[\"Deplete victim health\"]\n    C --&gt; C4[\"Check mission state\"]\n    C --&gt; D[\"Return observation dict\"]\n    D --&gt; E[\"GameObservation.process()&lt;br/&gt;Encode grid, position, status\"]\n    E --&gt; F[\"SAREnvGUI.render()\"]\n    F --&gt; F1[\"Camera.get_crop() \u2192 RGB frame\"]\n    F --&gt; F2[\"InfoPanel.update()\"]\n    F --&gt; F3[\"ChatPanel.update()\"]\n    F --&gt; F4[\"EdgeVignette.trigger(events)\"]</code></pre>"},{"location":"configuration/","title":"Configuration","text":""},{"location":"configuration/#configsexperimentyaml-full-experiment","title":"<code>configs/experiment.yaml</code> (full experiment)","text":"<pre><code>game:\n  display: 0              # Display index (0 = primary)\n  fullscreen: false\n  max_time: 10            # Mission time limit in minutes\n  prompt_type: sparse     # LLM prompt verbosity\n  openai_model: gpt-5-mini-2025-08-07\n  num_rows: 2              # Rooms tall\n  num_cols: 2              # Rooms wide\n  llm_nudge_interval: 50   # Steps between automatic LLM prompts\n\nvs:                        # Visual Search task config\n  ...\n\nmot:                       # Multi-Object Tracking task config\n  ...\n\nsurveys:                   # NASA-TLX and SART config\n  ...\n</code></pre>"},{"location":"configuration/#configsconfigyml-base-dev","title":"<code>configs/config.yml</code> (base / dev)","text":"<p>Simpler config used when running <code>python -m experiment.main</code> directly. Override individual parameters here for quick iteration.</p>"},{"location":"core-systems/","title":"Core Systems","text":""},{"location":"core-systems/#environment","title":"Environment","text":"<p>Class: <code>PickupVictimEnv</code> \u2014 <code>src/mosaic/sar/env.py</code></p> <p>The main Gymnasium-compatible environment. Inherits from <code>SARLevelGen</code>.</p> <p>Key configuration parameters:</p> Parameter Default Description <code>room_size</code> 14 Tiles per room (interior) <code>num_rows</code> 3 Rows of rooms <code>num_cols</code> 3 Columns of rooms <code>victim_placer</code> <code>VictimPlacer()</code> Placer controlling victim placement <code>lava_placer</code> <code>LavaPlacer()</code> Placer controlling lava placement <code>locked_room_placer</code> <code>LockedRoomPlacer()</code> Placer controlling locked doors/keys <code>agent_pov</code> True Enable first-person view camera <p>Key methods:</p> Method Description <code>gen_mission()</code> Generates a full level: rooms, doors, keys, lava, victims <code>reset()</code> Initializes an episode; computes <code>max_steps</code> <code>step(action)</code> Processes one action; updates victim health, checks end conditions <code>get_mission_status()</code> Returns <code>{status, saved_victims, remaining_victims}</code> <code>switch_camera(strategy)</code> Swaps camera strategy at runtime <p>Use the <code>build_sar_env()</code> factory (<code>src/mosaic/sar/env.py</code>) to construct a fully configured instance from already-built placer instances instead of calling <code>PickupVictimEnv(...)</code> directly.</p>"},{"location":"core-systems/#level-generation","title":"Level generation","text":"<p>Class: <code>SARLevelGen</code> \u2014 <code>src/mosaic/core/level.py</code></p> <p>Inherits from MiniGrid's <code>MiniGridEnv</code>. Provides:</p> <ul> <li>Multi-room grid construction</li> <li>Pluggable camera injection</li> <li>Base rendering with camera-aware crop</li> </ul> <p>Subclassed by <code>PickupVictimEnv</code> and <code>TutorialEnv</code>.</p>"},{"location":"core-systems/#victim-system","title":"Victim system","text":"<p>File: <code>src/mosaic/sar/objects.py</code></p>"},{"location":"core-systems/#real-victims-victim","title":"Real victims (<code>Victim</code>)","text":"<ul> <li>Rendered as a symmetric cross (\u271a)</li> <li>Directional variants: <code>victim_up</code>, <code>victim_down</code>, <code>victim_left</code>, <code>victim_right</code></li> <li>Have a <code>health</code> property (0.0 \u2192 1.0) with a rendered health bar</li> <li>Reward on rescue: +1.0</li> </ul>"},{"location":"core-systems/#fake-victims-fakevictim","title":"Fake victims (<code>FakeVictim</code>)","text":"<ul> <li>Rendered as an asymmetric T-shape (\u22a4)</li> <li>Left/right shift variants to vary appearance</li> <li>Same health depletion system as real victims</li> <li>Penalty on accidental rescue: -0.5</li> </ul>"},{"location":"core-systems/#placement-victimplacer-srcmosaicsarplacerspy","title":"Placement (<code>VictimPlacer</code>) \u2014 <code>src/mosaic/sar/placers.py</code>","text":"<ul> <li>Real victims placed preferentially in locked rooms (harder to reach)</li> <li>Fake victims distributed across all accessible rooms</li> <li>Reachability verified before placement (no inaccessible victims)</li> </ul>"},{"location":"core-systems/#hazards-obstacles","title":"Hazards &amp; obstacles","text":"<p>Lava (<code>LavaPlacer</code>) \u2014 <code>src/mosaic/sar/placers.py</code></p> <ul> <li>Placed randomly within rooms, avoiding doorways and agent spawn</li> <li>Stepping on lava immediately terminates the episode with failure</li> <li>Density controlled by the placer's configuration</li> </ul> <p>Locked doors &amp; keys</p> <ul> <li>A subset of room connections are randomly locked, controlled by <code>LockedRoomPlacer</code></li> <li>A matching colored key is placed in a reachable (unlocked) room</li> <li>The agent must carry the key to toggle the locked door open</li> </ul>"},{"location":"core-systems/#mission-system","title":"Mission system","text":"<p>Class: <code>PickupAllVictimsInstr</code> \u2014 <code>src/mosaic/sar/instructions.py</code></p> <p>Manages mission completion logic:</p> <ul> <li>Tracks rescued vs. remaining real victims</li> <li>Reports <code>success</code> when all real victims rescued</li> <li>Reports <code>failure</code> on lava contact or timeout</li> </ul>"},{"location":"core-systems/#camera-system","title":"Camera system","text":"<p>File: <code>src/mosaic/core/camera.py</code></p> <p>Interchangeable camera strategies:</p> <ul> <li><code>FullviewCamera</code> \u2014 Shows the entire grid at once. Best for small maps or debugging.</li> <li><code>AgentCenteredCamera</code> \u2014 Centers the viewport on the agent. Entire room plus border tiles always visible.</li> <li><code>EdgeFollowCamera</code> (default) \u2014 Camera moves only when the agent approaches the edge of the viewport (dead-zone tracking).</li> <li><code>AgentFOVCamera</code> \u2014 Clips the view to the boundaries of the agent's current room. Natural wall framing.</li> <li><code>AgentConeCamera</code> \u2014 Room-bounded view with MiniGrid's line-of-sight cone; tiles outside the agent's forward sightline are blacked out.</li> </ul> <p>Switch cameras at runtime:</p> <pre><code>env.switch_camera(\"edge_follow\")   # or \"full\", \"centered\", \"fov\", \"cone\"\n</code></pre>"},{"location":"core-systems/#observation-encoding","title":"Observation encoding","text":"<p>Class: <code>GameObservation</code> \u2014 <code>src/mosaic/sar/observations.py</code></p> <p>Returns a dict on each step:</p> Key Type Description <code>image</code> ndarray (H\u00d7W\u00d73) RGB camera frame <code>direction</code> int Agent facing: 0=E, 1=S, 2=W, 3=N <code>agent_x</code>, <code>agent_y</code> int Agent grid position <code>grid</code> ndarray (H\u00d7W) Full map encoded as integers (see below) <code>carrying</code> str or None Color of held object <code>step_count</code> int Steps elapsed this episode <code>max_steps</code> int Episode step limit <code>mission_status</code> str <code>\"success\"</code>, <code>\"failure\"</code>, or <code>\"incomplete\"</code> <code>saved_victims</code> int Count of rescued real victims <code>remaining_victims</code> int Count of unrescued real victims <code>num_rows</code>, <code>num_cols</code> int Map dimensions in rooms <code>room_size</code> int Room size in tiles <code>cam_top_x</code>, <code>cam_top_y</code> int Camera viewport top-left tile <code>cam_view_w</code>, <code>cam_view_h</code> int Camera viewport size in tiles <p>Grid integer encoding:</p> <pre><code>0       = Empty floor\n1       = Wall\n4       = Lava\n5       = Victim (real)\n6       = FakeVictim (decoy)\n\nDoors:  10 + (color_index \u00d7 3) + door_state\n  door_state: 0=open, 1=closed, 2=locked\n  color_index: red=0, green=1, blue=2, purple=3, yellow=4, grey=5\n  Range: 10-27\n\nKeys:   30 + color_index\n  Range: 30-35\n</code></pre>"},{"location":"core-systems/#reward-system","title":"Reward system","text":"<p>Class: <code>RescueAction</code> \u2014 <code>src/mosaic/sar/actions.py</code></p> <p>All rewards are sparse (returned only on specific events):</p> Event Reward Rescue a real victim +1.0 Pick up a fake victim -0.5 All victims rescued (mission complete) +1.0 bonus Step on lava episode terminated, no reward Timeout episode terminated, no reward"},{"location":"experiment/","title":"Experiment Framework","text":"<p>File: <code>src/experiment/experiment_main.py</code></p>"},{"location":"experiment/#task-sequence","title":"Task sequence","text":"<pre><code>flowchart TD\n    A[\"Tobii Calibration\"] --&gt; B[\"Visual Search Task&lt;br/&gt;(baseline cognitive measure)\"]\n    B --&gt; C[\"Multi-Object Tracking Task&lt;br/&gt;(baseline cognitive measure)\"]\n    C --&gt; D[\"Tutorial&lt;br/&gt;(single-room practice)\"]\n    D --&gt; E[\"Main SAR Game&lt;br/&gt;(primary task, with LLM assistant)\"]\n    E --&gt; F[\"SART Survey&lt;br/&gt;(situation awareness)\"]\n    F --&gt; G[\"NASA-TLX Survey&lt;br/&gt;(perceived workload)\"]</code></pre>"},{"location":"experiment/#data-collection","title":"Data collection","text":"<p>Every frame during the main game is recorded as a <code>SARGameTrial</code> object streamed via LSL (Lab Streaming Layer):</p> <ul> <li>Full observation dict (grid state, agent position, camera bounds)</li> <li>Action taken and resulting reward</li> <li>LLM prompt + response (if any) with timing</li> <li>Eye gaze coordinates (via Tobii LSL bridge)</li> <li>Experiment metadata (participant ID, trial number, task order)</li> </ul> <p>This enables full post-hoc replay and analysis without re-running the experiment.</p>"},{"location":"experiment/#eye-tracking-integration","title":"Eye-tracking integration","text":"<p>Package: <code>ixp</code> (internal experiment framework) + <code>tobii-research</code></p> <ul> <li><code>TobiiEyeTracker</code> is initialized and calibrated before the experiment begins</li> <li>Optional recalibration between tasks</li> <li>Gaze data is streamed in sync with game state via LSL</li> <li>Area of Interest (AOI) analysis can be run post-hoc using recorded gaze + frame data</li> </ul> <p>The game can run without a Tobii device; eye-tracking is automatically skipped if the hardware is not detected.</p>"},{"location":"experiment/#replay-system","title":"Replay system","text":"<p>File: <code>src/experiment/replay.py</code></p> <p>Reads a JSON-encoded LSL recording and replays the session in a Pygame window.</p> <pre><code>PYTHONPATH=src python -m experiment.replay --file &lt;recording.json&gt;\n</code></pre> <p>Playback controls:</p> Key Action <code>Space</code> Pause / resume <code>\u2190</code> Step back one frame <code>\u2192</code> Step forward one frame <code>ESC</code> Quit <p>The replay reconstructs each frame from the encoded <code>grid</code> field in the recording \u2014 no environment re-execution required.</p>"},{"location":"experiment/#this-studys-tuning","title":"This study's tuning","text":"<p><code>experiment/</code> wires study-specific behavior on top of <code>mosaic</code>'s neutral defaults:</p> <ul> <li><code>experiment.placers.LavaRiskVictimPlacer</code> \u2014 this study's health/decay tuning for victim placement.</li> <li><code>experiment.pacing.TunedPickupVictimEnv</code> \u2014 this study's pacing formula, passed as <code>env_cls</code> to <code>build_sar_env()</code>.</li> <li><code>experiment.game.SARGame</code> \u2014 the task that streams <code>SARGameTrial</code> records via LSL, wiring the tuned placers and pacing into <code>mosaic</code>'s injection points.</li> </ul>"},{"location":"game-concept/","title":"Game Concept &amp; Mechanics","text":""},{"location":"game-concept/#objective","title":"Objective","text":"<p>Rescue all real victims before time runs out. Real victims are displayed as a cross shape (\u271a). Fake victims (decoys) appear as a T-shape (\u22a4) \u2014 picking them up applies a score penalty.</p>"},{"location":"game-concept/#building-layout","title":"Building layout","text":"<p>The building is a grid of rooms (default 2x2 or 3x3), each separated by walls with doorways. Some doors are locked and require a matching colored key found elsewhere in the building.</p>"},{"location":"game-concept/#hazards","title":"Hazards","text":"Hazard Effect Lava Instant mission failure if stepped on Locked doors Block passage until matching key is collected Time limit Mission fails if max steps exceeded"},{"location":"game-concept/#victim-health","title":"Victim health","text":"<p>All victims (real and fake) have a health bar that depletes over time while they are visible to the agent. This creates urgency \u2014 visible victims that are not rescued will eventually be lost.</p>"},{"location":"game-concept/#scoring","title":"Scoring","text":"Event Reward Rescue real victim +1.0 Pick up fake victim (decoy) -0.5 Complete mission (all rescued) +1.0 bonus"},{"location":"game-concept/#controls","title":"Controls","text":"Key Action <code>\u2191</code> or <code>W</code> Move forward <code>\u2190</code> Rotate left <code>\u2192</code> Rotate right <code>Space</code> Toggle / interact (open doors) <code>Tab</code> or <code>Page Up</code> Pick up / rescue victim <code>Left Shift</code> or <code>Page Down</code> Drop held object <code>Alt</code> (left or right) Ask LLM for a suggestion <code>F11</code> Toggle fullscreen <code>Backspace</code> Reset current mission <code>ESC</code> Quit"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.9+</li> <li>A display server (X11 or native on macOS/Windows)</li> <li>Optional: Tobii eye tracker + <code>tobii-research</code> SDK for eye-tracking experiments</li> </ul>"},{"location":"getting-started/#install","title":"Install","text":"<pre><code>git clone https://github.com/iHuman-Lab/mosaic.git\ncd mosaic\n\n# Install dependencies\npip install -r requirements.txt\n\n# Or install as a package\npip install -e .\n</code></pre>"},{"location":"getting-started/#api-keys-for-llm-features","title":"API keys (for LLM features)","text":"<p>Set environment variables for your chosen LLM provider:</p> <pre><code>export OPENAI_API_KEY=\"sk-...\"\nexport GOOGLE_API_KEY=\"...\"\n</code></pre>"},{"location":"getting-started/#running-the-game","title":"Running the game","text":"<p><code>experiment/</code> is excluded from the installable package, so its entry points are run as modules (<code>python -m</code>) from the repo root, with <code>src/</code> on <code>PYTHONPATH</code>:</p>"},{"location":"getting-started/#interactive-mode-play-the-game","title":"Interactive mode (play the game)","text":"<pre><code>PYTHONPATH=src python -m experiment.main\n</code></pre> <p>Launches the full Pygame GUI with a 2x2 room grid, real/fake victims, lava, locked rooms, and an optional LLM assistant.</p>"},{"location":"getting-started/#experiment-mode-research","title":"Experiment mode (research)","text":"<pre><code>PYTHONPATH=src python -m experiment.experiment_main\n</code></pre> <p>Runs the full experiment protocol: visual search task, multi-object tracking, tutorial, main SAR game, and cognitive surveys. Requires Tobii hardware and the <code>ixp</code> package.</p>"},{"location":"getting-started/#replay-a-session","title":"Replay a session","text":"<pre><code>PYTHONPATH=src python -m experiment.replay --file &lt;path_to_lsl_recording.json&gt;\n</code></pre>"},{"location":"getting-started/#a-minimal-script","title":"A minimal script","text":"<pre><code>from mosaic.sar.env import build_sar_env\nfrom mosaic.sar.placers import VictimPlacer\nfrom mosaic.gui.main import SAREnvGUI\n\nenv = build_sar_env(\n    screen_size=800,\n    victim_placer=VictimPlacer(num_real_victims=3),\n)\ngui = SAREnvGUI(env, config={\"fullscreen\": False})\ngui.run()\n</code></pre> <p>See Architecture for how <code>mosaic/</code> (generic, installable) and <code>experiment/</code> (this lab's calibrated study code) are separated.</p>"},{"location":"gui/","title":"GUI","text":""},{"location":"gui/#main-window-sarenvgui-srcmosaicguimainpy","title":"Main window (<code>SAREnvGUI</code>) \u2014 <code>src/mosaic/gui/main.py</code>","text":"<p>The top-level Pygame controller. Layout:</p> <pre><code>block-beta\ncolumns 2\nviewport[\"Game Viewport (800 \u00d7 800 px)\"]\ninfo[\"Info Panel (400 px)\"]\nspace:1\nchat[\"Chat Panel (LLM msgs)\"]</code></pre> <ul> <li>30 FPS rendering loop</li> <li><code>F11</code> toggles fullscreen with dynamic UI scaling</li> <li><code>pygame_gui</code> used for all UI widgets</li> </ul> <p>Every major component is constructor-injectable \u2014 <code>llm_client</code>, <code>prompt_builder</code>, <code>response_processor</code>, <code>user</code>, <code>info_panel</code>, <code>chat_panel</code>, and <code>vignette</code> all default to the neutral <code>mosaic</code> implementations but can be swapped by passing already-built instances:</p> <pre><code>gui = SAREnvGUI(\n    env,\n    config={\"fullscreen\": False},\n    llm_client=my_llm_client,\n    vignette=EdgeVignette(game_size, styles=custom_styles),\n)\n</code></pre> <p>A fullscreen toggle only rebinds <code>info_panel</code>/<code>chat_panel</code> to a fresh <code>UIManager</code>; it does not recreate <code>user</code>, <code>vignette</code>, or the panels themselves, so external references to those instances survive a toggle.</p>"},{"location":"gui/#infopanel-srcmosaicguiinfopy","title":"InfoPanel \u2014 <code>src/mosaic/gui/info.py</code>","text":"<p>Displays mission-critical information on the right panel:</p> <ul> <li>Mission Status header (victims saved / remaining)</li> <li>Metrics: cumulative reward, step count, elapsed time</li> <li>Object Table: lists all game objects in range with type, color, location, visibility   (in current camera view or not), reachability (BFS distance, or blocked), and whether a tool   (key) is required</li> <li>Compass: cardinal direction indicator for agent facing</li> <li>Controls legend: key bindings reminder</li> </ul>"},{"location":"gui/#chatpanel-srcmosaicguichatpy","title":"ChatPanel \u2014 <code>src/mosaic/gui/chat.py</code>","text":"<p>Displays messages from the LLM assistant:</p> <ul> <li>New messages cause a blinking highlight effect</li> <li>Message colors: agent suggestions in cornflower blue, errors in crimson</li> <li>Polls for async LLM responses each frame</li> </ul>"},{"location":"gui/#edge-vignette-feedback-srcmosaicguifeedbackpy","title":"Edge vignette feedback \u2014 <code>src/mosaic/gui/feedback.py</code>","text":"<p><code>EdgeVignette</code> renders a configurable screen-edge glow in response to game events (e.g. taking damage, nearing a hazard). It's triggered from <code>SAREnvGUI</code> via <code>user.on_step</code> and is injected into <code>SAREnvGUI</code> like any other component, so its colors/behavior can be overridden per study.</p>"},{"location":"gui/#user-input-user-srcmosaicguiuserpy","title":"User input (<code>User</code>) \u2014 <code>src/mosaic/gui/user.py</code>","text":"<p>Handles all keyboard input and dispatches:</p> <ul> <li>Movement and interaction actions to <code>env.step()</code></li> <li>LLM queries on <code>Alt</code> key (runs in a background thread to avoid frame drops)</li> <li>Episode reset on <code>Backspace</code></li> </ul>"},{"location":"llm/","title":"LLM Integration","text":"<p>MOSAIC separates the contract (in <code>mosaic</code>) from provider wiring (in <code>experiment</code>):</p> <ul> <li><code>mosaic.llm.client.LLMClient</code> \u2014 abstract base class; any implementation just needs <code>query(prompt) -&gt; str</code>.</li> <li><code>mosaic.llm.client.DummyLLMClient</code> \u2014 a no-op implementation for testing/demoing without API keys.</li> <li><code>experiment.llm.LlamaIndexLLMClient</code> \u2014 concrete implementation backed by <code>llama_index</code>, supporting OpenAI and Google providers.</li> <li><code>experiment.llm.build_llm_client(provider, model)</code> \u2014 factory that returns a <code>DummyLLMClient</code> when no provider is configured, otherwise a configured <code>LlamaIndexLLMClient</code>.</li> </ul> <pre><code>from experiment.llm import build_llm_client\n\nclient = build_llm_client(provider=\"openai\", model=\"gpt-4o-mini\")\nresponse = client.query(prompt_string)\n</code></pre>"},{"location":"llm/#supported-providers","title":"Supported providers","text":"Provider Example models OpenAI <code>gpt-4o-mini</code>, <code>gpt-5-mini-2025-08-07</code> Google <code>gemini-2.5-flash</code> Dummy No-op client for testing without API keys"},{"location":"llm/#how-it-works","title":"How it works","text":"<ol> <li>Player presses <code>Alt</code> (or the auto-nudge interval fires).</li> <li><code>mosaic/llm/process_prompts.py</code> builds a structured prompt from the current <code>GameObservation</code>:    agent position and facing direction, visible/reachable objects with pathfinding distances,    current mission status, and prompt type (<code>sparse</code> = minimal, <code>detailed</code> = full grid scan + strategy).</li> <li>The prompt is sent to the configured <code>LLMClient</code> asynchronously.</li> <li><code>mosaic/llm/parser.py</code> cleans the response (removes coordinates, reformats object labels).</li> <li>The cleaned suggestion appears in the <code>ChatPanel</code>.</li> </ol>"},{"location":"llm/#configuration","title":"Configuration","text":"<pre><code># configs/experiment.yaml\ngame:\n  prompt_type: sparse          # \"sparse\" or \"detailed\"\n  openai_model: gpt-5-mini-2025-08-07\n  llm_nudge_interval: 50       # auto-prompt every N steps (0 = disabled)\n</code></pre> <p>Tests exercise <code>LLMClient</code> consumers by passing a fake implementation through the constructor (dependency injection) rather than monkeypatching the client.</p>"},{"location":"testing/","title":"Testing","text":"<pre><code>pytest tests/\n</code></pre> <p>MOSAIC's tests favor dependency injection over monkeypatching: seams like <code>LLMClient</code> are exercised with fakes passed directly into constructors, not by patching module attributes.</p> Test file What it validates <code>test_environment.py</code> Environment initialization, step, reset <code>test_actions.py</code> <code>RescueAction</code>'s SAR event emission (<code>info[\"events\"]</code>) <code>test_lava.py</code> Lava placement doesn't block all paths <code>test_lava_50_percent.py</code> High-density lava placement edge cases <code>test_reachability.py</code> All objects (victims, keys) are reachable from spawn <code>test_strict_reachability.py</code> Strict BFS reachability with <code>unblocking=False</code> <code>test_victim_placement.py</code> Split between generic victim placement and study calibration <code>test_pacing.py</code> <code>TunedPickupVictimEnv</code>'s victim-health decay (the only decay tick in the codebase) <code>test_llm_api.py</code> The <code>mosaic.llm.client</code> contract: <code>LLMClient</code>, <code>DummyLLMClient</code>, and the <code>ask()</code> pipeline \u2014 no providers, no real requests <code>test_experiment_llm.py</code> <code>experiment.llm</code> provider selection, model defaults, and <code>LlamaIndexLLMClient</code>, via a fake object \u2014 never calls the real <code>get_llm()</code> <code>test_gui_api.py</code> The GUI/LLM dependency-injection boundary: <code>User</code>, <code>SAREnvGUI</code>, <code>ChatPanel</code> receive an already-built <code>LLMClient</code> <code>test_gui_feedback.py</code> <code>EdgeVignette</code> feedback behavior <code>playground.py</code> Manual development / debugging entrypoint"},{"location":"api/","title":"API Reference","text":"<p>Auto-generated from source docstrings, grouped by area:</p> <ul> <li>Environment \u2014 <code>mosaic.sar.env</code></li> <li>Core \u2014 camera strategies, level generation, placer base classes</li> <li>SAR Components \u2014 victims, actions, observations, instructions, placers</li> <li>GUI \u2014 <code>SAREnvGUI</code> controller and feedback vignette</li> <li>LLM \u2014 <code>LLMClient</code> contract</li> </ul>"},{"location":"api/core/","title":"Core","text":""},{"location":"api/core/#mosaic.core.level","title":"level","text":"<p>Classes:</p> Name Description <code>SARLevelGen</code> <p>Search and Rescue level generator with pluggable camera system.</p>"},{"location":"api/core/#mosaic.core.level.SARLevelGen","title":"SARLevelGen","text":"<pre><code>SARLevelGen(room_size=13, num_rows=5, num_cols=5, num_dists=18, locked_room_prob=0.5, locations=True, unblocking=True, implicit_unlock=True, action_kinds=['goto', 'pickup', 'open', 'putnext'], instr_kinds=['action', 'and', 'seq'], camera_strategy=None, **kwargs)\n</code></pre> <p>               Bases: <code>LevelGen</code></p> <p>Search and Rescue level generator with pluggable camera system.</p> <p>Methods:</p> Name Description <code>get_camera_view</code> <p>Get current camera view using the configured strategy.</p> <code>render</code> <p>Render the environment.</p> <code>switch_camera</code> <p>Switch to a different camera strategy at runtime.</p>"},{"location":"api/core/#mosaic.core.level.SARLevelGen.get_camera_view","title":"get_camera_view","text":"<pre><code>get_camera_view() -&gt; np.ndarray\n</code></pre> <p>Get current camera view using the configured strategy.</p>"},{"location":"api/core/#mosaic.core.level.SARLevelGen.render","title":"render","text":"<pre><code>render()\n</code></pre> <p>Render the environment.</p>"},{"location":"api/core/#mosaic.core.level.SARLevelGen.switch_camera","title":"switch_camera","text":"<pre><code>switch_camera(camera_strategy: CameraStrategy)\n</code></pre> <p>Switch to a different camera strategy at runtime.</p>"},{"location":"api/core/#mosaic.core.camera","title":"camera","text":"<p>Classes:</p> Name Description <code>AgentCenteredCamera</code> <p>Camera that stays centered on the agent's room.</p> <code>AgentConeCamera</code> <p>Shows the agent's current room with a forward-cone blackout.</p> <code>AgentFOVCamera</code> <p>Shows only the agent's current room.</p> <code>CameraConfig</code> <p>Configuration for camera behavior.</p> <code>CameraStrategy</code> <p>Abstract base class for different camera behaviors.</p> <code>EdgeFollowCamera</code> <p>Camera that follows the agent with a dead-zone margin.</p>"},{"location":"api/core/#mosaic.core.camera.AgentCenteredCamera","title":"AgentCenteredCamera","text":"<pre><code>AgentCenteredCamera(extra_tiles=(4, 4), tile_size=32)\n</code></pre> <p>               Bases: <code>CameraStrategy</code></p> <p>Camera that stays centered on the agent's room.</p> <p>Methods:</p> Name Description <code>get_crop</code> <p>Get a crop centered on the agent's current room.</p>"},{"location":"api/core/#mosaic.core.camera.AgentCenteredCamera.get_crop","title":"get_crop","text":"<pre><code>get_crop(grid, agent_pos, agent_dir, room=None, **kwargs) -&gt; np.ndarray\n</code></pre> <p>Get a crop centered on the agent's current room.</p>"},{"location":"api/core/#mosaic.core.camera.AgentConeCamera","title":"AgentConeCamera","text":"<pre><code>AgentConeCamera(config: CameraConfig = None)\n</code></pre> <p>               Bases: <code>AgentFOVCamera</code></p> <p>Shows the agent's current room with a forward-cone blackout.</p> <p>Extends AgentFOVCamera: same room crop, but tiles outside MiniGrid's built-in forward visibility cone are rendered black.</p>"},{"location":"api/core/#mosaic.core.camera.AgentFOVCamera","title":"AgentFOVCamera","text":"<pre><code>AgentFOVCamera(config: CameraConfig = None)\n</code></pre> <p>               Bases: <code>CameraStrategy</code></p> <p>Shows only the agent's current room.</p> <p>The crop is always room_size \u00d7 room_size tiles (always square). Walls between rooms naturally prevent the agent from seeing into adjacent rooms \u2014 no additional fog-of-war mask is needed.</p>"},{"location":"api/core/#mosaic.core.camera.CameraConfig","title":"CameraConfig  <code>dataclass</code>","text":"<pre><code>CameraConfig(view_tiles: tuple[int, int] = (8, 8), margin: int = 3, tile_size: int = 64)\n</code></pre> <p>Configuration for camera behavior.</p>"},{"location":"api/core/#mosaic.core.camera.CameraStrategy","title":"CameraStrategy","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for different camera behaviors.</p> <p>Methods:</p> Name Description <code>get_crop</code> <p>Return a cropped view of the grid.</p> <code>get_visible_bounds</code> <p>Return (x0, y0, x1, y1) tile bounds currently visible. Default: full grid.</p>"},{"location":"api/core/#mosaic.core.camera.CameraStrategy.get_crop","title":"get_crop  <code>abstractmethod</code>","text":"<pre><code>get_crop(grid, agent_pos, agent_dir, **kwargs) -&gt; np.ndarray\n</code></pre> <p>Return a cropped view of the grid.</p>"},{"location":"api/core/#mosaic.core.camera.CameraStrategy.get_visible_bounds","title":"get_visible_bounds","text":"<pre><code>get_visible_bounds(grid_width: int, grid_height: int) -&gt; tuple[int, int, int, int]\n</code></pre> <p>Return (x0, y0, x1, y1) tile bounds currently visible. Default: full grid.</p>"},{"location":"api/core/#mosaic.core.camera.EdgeFollowCamera","title":"EdgeFollowCamera","text":"<pre><code>EdgeFollowCamera(config: CameraConfig = None)\n</code></pre> <p>               Bases: <code>CameraStrategy</code></p> <p>Camera that follows the agent with a dead-zone margin.</p> <p>Methods:</p> Name Description <code>get_crop</code> <p>Get a crop that follows the agent with edge-following behavior.</p> <code>reset</code> <p>Reset camera state.</p>"},{"location":"api/core/#mosaic.core.camera.EdgeFollowCamera.get_crop","title":"get_crop","text":"<pre><code>get_crop(grid, agent_pos, agent_dir, grid_width=None, grid_height=None, **kwargs) -&gt; np.ndarray\n</code></pre> <p>Get a crop that follows the agent with edge-following behavior.</p>"},{"location":"api/core/#mosaic.core.camera.EdgeFollowCamera.reset","title":"reset","text":"<pre><code>reset()\n</code></pre> <p>Reset camera state.</p>"},{"location":"api/core/#mosaic.core.placers","title":"placers","text":"<p>Classes:</p> Name Description <code>Placer</code> <p>Places objects into a generated level.</p>"},{"location":"api/core/#mosaic.core.placers.Placer","title":"Placer","text":"<p>               Bases: <code>ABC</code></p> <p>Places objects into a generated level.</p> <p>Implementations mutate the level in place and return nothing, either by writing to <code>level_gen.grid</code> directly or through the level generator's helpers. Called from the environment's <code>gen_mission()</code>.</p> <p>Methods:</p> Name Description <code>place_all</code> <p>Place this components's objects into the grid.</p> <code>reset</code> <p>Optional per-episode reset hook. No-op by default.</p>"},{"location":"api/core/#mosaic.core.placers.Placer.place_all","title":"place_all  <code>abstractmethod</code>","text":"<pre><code>place_all(level_gen, num_rows: int, num_cols: int, **kwargs) -&gt; None\n</code></pre> <p>Place this components's objects into the grid.</p>"},{"location":"api/core/#mosaic.core.placers.Placer.reset","title":"reset","text":"<pre><code>reset(level_gen) -&gt; None\n</code></pre> <p>Optional per-episode reset hook. No-op by default.</p>"},{"location":"api/environment/","title":"Environment","text":""},{"location":"api/environment/#mosaic.sar.env","title":"env","text":"<p>Classes:</p> Name Description <code>PickupVictimEnv</code> <p>Functions:</p> Name Description <code>build_sar_env</code> <p>Factory that creates a fully configured PickupVictimEnv (or a subclass,</p>"},{"location":"api/environment/#mosaic.sar.env.PickupVictimEnv","title":"PickupVictimEnv","text":"<pre><code>PickupVictimEnv(room_size=14, num_rows=3, num_cols=3, num_dists=18, unblocking=False, victim_placer: Placer = None, locked_room_placer: Placer = None, lava_placer: Placer = None, action: BaseAction = None, observation: ObservationProcessor = None, **kwargs)\n</code></pre> <p>               Bases: <code>SARLevelGen</code></p> <p>Methods:</p> Name Description <code>calculate_max_steps</code> <p>Compute this episode's max_steps. Override in a subclass for custom</p> <code>gen_mission</code> <p>Generate the mission layout and instructions.</p> <code>get_all_victims</code> <p>Returns a list of all victim objects currently present in the environment.</p> <code>get_mission_status</code> <p>Returns the current mission status.</p> <code>num_navs_needed</code> <p>Override to handle custom instruction types.</p> <code>reset</code> <p>Reset the environment and all stats.</p> <code>validate_instrs</code> <p>Override to handle custom instruction types.</p>"},{"location":"api/environment/#mosaic.sar.env.PickupVictimEnv.calculate_max_steps","title":"calculate_max_steps","text":"<pre><code>calculate_max_steps()\n</code></pre> <p>Compute this episode's max_steps. Override in a subclass for custom pacing. Default: None \u2014 MiniGrid's own generic max-steps fallback (see RoomGridLevel.reset()) stands unmodified.</p>"},{"location":"api/environment/#mosaic.sar.env.PickupVictimEnv.gen_mission","title":"gen_mission","text":"<pre><code>gen_mission()\n</code></pre> <p>Generate the mission layout and instructions.</p>"},{"location":"api/environment/#mosaic.sar.env.PickupVictimEnv.get_all_victims","title":"get_all_victims","text":"<pre><code>get_all_victims()\n</code></pre> <p>Returns a list of all victim objects currently present in the environment.</p>"},{"location":"api/environment/#mosaic.sar.env.PickupVictimEnv.get_mission_status","title":"get_mission_status","text":"<pre><code>get_mission_status()\n</code></pre> <p>Returns the current mission status.</p> <p>Returns:</p> Name Type Description <code>dict</code> <p>Dictionary containing: - 'status': 'success', 'failure', or 'incomplete' - 'saved_victims': Number of victims saved - 'total_victims': Total number of victims in the mission - 'remaining_victims': Number of victims left to save</p>"},{"location":"api/environment/#mosaic.sar.env.PickupVictimEnv.num_navs_needed","title":"num_navs_needed","text":"<pre><code>num_navs_needed(instrs)\n</code></pre> <p>Override to handle custom instruction types.</p> <p>Parameters:</p> Name Type Description Default <code>instrs</code> <p>Instruction object</p> required <p>Returns:</p> Name Type Description <code>int</code> <p>Number of navigation actions needed (for mission generation)</p>"},{"location":"api/environment/#mosaic.sar.env.PickupVictimEnv.reset","title":"reset","text":"<pre><code>reset(**kwargs)\n</code></pre> <p>Reset the environment and all stats.</p>"},{"location":"api/environment/#mosaic.sar.env.PickupVictimEnv.validate_instrs","title":"validate_instrs","text":"<pre><code>validate_instrs(instrs)\n</code></pre> <p>Override to handle custom instruction types.</p> <p>Parameters:</p> Name Type Description Default <code>instrs</code> <p>Instruction object to validate</p> required"},{"location":"api/environment/#mosaic.sar.env.build_sar_env","title":"build_sar_env","text":"<pre><code>build_sar_env(screen_size: int, room_size=14, num_rows: int = 5, num_cols: int = 5, tile_size: int = 64, victim_placer: Placer = None, lava_placer: Placer = None, locked_room_placer: Placer = None, env_cls=None, **kwargs) -&gt; PickupVictimEnv\n</code></pre> <p>Factory that creates a fully configured PickupVictimEnv (or a subclass, via env_cls \u2014 e.g. to override calculate_max_steps() with custom pacing).</p> <p>Each placer is passed as an already-constructed instance \u2014 configure it yourself (e.g. VictimPlacer(num_real_victims=6)) before passing it in. Defaults to each placer's base class, unconfigured, when omitted.</p>"},{"location":"api/gui/","title":"GUI","text":""},{"location":"api/gui/#mosaic.gui.main","title":"main","text":"<p>Classes:</p> Name Description <code>SAREnvGUI</code>"},{"location":"api/gui/#mosaic.gui.main.SAREnvGUI","title":"SAREnvGUI","text":"<pre><code>SAREnvGUI(env, config: Optional[dict] = None, llm_client=None, prompt_builder=None, response_processor=None, user=None, info_panel=None, chat_panel=None, vignette=None)\n</code></pre> <p>Methods:</p> Name Description <code>set_advice_callbacks</code> <p>Configure study-specific behavior (e.g. battery reveal) to run on</p> <code>toggle_fullscreen</code> <p>Toggle between fullscreen and windowed mode.</p>"},{"location":"api/gui/#mosaic.gui.main.SAREnvGUI.set_advice_callbacks","title":"set_advice_callbacks","text":"<pre><code>set_advice_callbacks(on_reply=None, on_end=None)\n</code></pre> <p>Configure study-specific behavior (e.g. battery reveal) to run on advice reply/blink-end. chat_panel is a single instance for the life of the GUI, so this persists automatically across fullscreen toggles.</p>"},{"location":"api/gui/#mosaic.gui.main.SAREnvGUI.toggle_fullscreen","title":"toggle_fullscreen","text":"<pre><code>toggle_fullscreen()\n</code></pre> <p>Toggle between fullscreen and windowed mode.</p>"},{"location":"api/gui/#mosaic.gui.feedback","title":"feedback","text":"<p>Classes:</p> Name Description <code>EdgeVignette</code> <p>A brief, translucent perimeter-vignette flash over the square game</p> <code>VignetteStyle</code> <p>One event type's vignette appearance. Frozen because instances are</p> <p>Functions:</p> Name Description <code>select_event</code> <p>Pick the single highest-priority event to render \u2014 priority comes</p>"},{"location":"api/gui/#mosaic.gui.feedback.EdgeVignette","title":"EdgeVignette","text":"<pre><code>EdgeVignette(size: int, clock_ms=None, falloff_fraction: float = 0.225, center_strength: float = 0.065, styles: Optional[Dict[str, VignetteStyle]] = None)\n</code></pre> <p>A brief, translucent perimeter-vignette flash over the square game viewport, color/duration/shape keyed off SAR event type. The gradient mask and per-color tinted surfaces are built once and reused every frame \u2014 only a cheap set_alpha() + blit happens per render() call.</p>"},{"location":"api/gui/#mosaic.gui.feedback.VignetteStyle","title":"VignetteStyle  <code>dataclass</code>","text":"<pre><code>VignetteStyle(color: Tuple[int, int, int], peak_alpha: int, duration: float, envelope: str, priority: int = 99)\n</code></pre> <p>One event type's vignette appearance. Frozen because instances are shared (by reference) across every EdgeVignette via DEFAULT_VIGNETTE_STYLES \u2014 mutating one in place would corrupt it for every instance. Use dataclasses.replace() for a one-off tweak of an existing style.</p>"},{"location":"api/gui/#mosaic.gui.feedback.select_event","title":"select_event","text":"<pre><code>select_event(events: list, styles: Dict[str, VignetteStyle]) -&gt; Optional[dict]\n</code></pre> <p>Pick the single highest-priority event to render \u2014 priority comes from each event's resolved VignetteStyle, so an event with no resolvable style (absent from both defaults and overrides) is filtered out entirely, not just deprioritized. Returns None if no event in the list has a resolvable style. Ties are broken positionally: min() returns the first minimal element, so whichever supported event appears earlier in the list wins.</p>"},{"location":"api/llm/","title":"LLM","text":""},{"location":"api/llm/#mosaic.llm.client","title":"client","text":"<p>Classes:</p> Name Description <code>DummyLLMClient</code> <p>No-LLM fallback \u2014 returns a neutral message, no network.</p> <code>LLMClient</code> <p>Text-in, text-out contract for talking to an LLM backend.</p>"},{"location":"api/llm/#mosaic.llm.client.DummyLLMClient","title":"DummyLLMClient","text":"<p>               Bases: <code>LLMClient</code></p> <p>No-LLM fallback \u2014 returns a neutral message, no network.</p>"},{"location":"api/llm/#mosaic.llm.client.LLMClient","title":"LLMClient","text":"<p>               Bases: <code>ABC</code></p> <p>Text-in, text-out contract for talking to an LLM backend.</p> <p>Deliberately knows nothing about SAR observations, prompts, GUI panels, game actions, or experiment conditions \u2014 and nothing about providers or model names, since those aren't universal properties of an LLM client.</p> <p>Methods:</p> Name Description <code>query</code> <p>Send a text prompt and return text.</p>"},{"location":"api/llm/#mosaic.llm.client.LLMClient.query","title":"query  <code>abstractmethod</code>","text":"<pre><code>query(prompt: str) -&gt; str\n</code></pre> <p>Send a text prompt and return text.</p>"},{"location":"api/sar/","title":"SAR Components","text":""},{"location":"api/sar/#mosaic.sar.objects","title":"objects","text":"<p>Classes:</p> Name Description <code>FakeVictim</code> <p>Fake victim with asymmetric T-shape.</p> <code>Victim</code> <p>Real victim with symmetric cross shape.</p> <code>VictimBase</code> <p>Base class for all victim objects with common functionality.</p>"},{"location":"api/sar/#mosaic.sar.objects.FakeVictim","title":"FakeVictim","text":"<pre><code>FakeVictim(shift, direction, color='red')\n</code></pre> <p>               Bases: <code>VictimBase</code></p> <p>Fake victim with asymmetric T-shape.</p> <p>Create a fake victim.</p> <p>Parameters:</p> Name Type Description Default <code>shift</code> <p>Shift direction (\"left\" or \"right\")</p> required <code>direction</code> <p>Direction the T is pointing (\"up\", \"down\", \"left\", \"right\")</p> required <code>color</code> <p>Color of the fake victim (default \"red\")</p> <code>'red'</code>"},{"location":"api/sar/#mosaic.sar.objects.Victim","title":"Victim","text":"<pre><code>Victim(direction, color='red')\n</code></pre> <p>               Bases: <code>VictimBase</code></p> <p>Real victim with symmetric cross shape.</p> <p>Create a victim.</p> <p>Parameters:</p> Name Type Description Default <code>direction</code> <p>Direction the victim is facing (\"up\", \"down\", \"left\", \"right\")</p> required <code>color</code> <p>Color of the victim (default \"red\")</p> <code>'red'</code>"},{"location":"api/sar/#mosaic.sar.objects.VictimBase","title":"VictimBase","text":"<pre><code>VictimBase(*args, **kwargs)\n</code></pre> <p>               Bases: <code>WorldObj</code></p> <p>Base class for all victim objects with common functionality.</p> <p>Methods:</p> Name Description <code>can_overlap</code> <p>Victims cannot be walked over.</p> <code>can_pickup</code> <p>Victims can be picked up.</p> <code>deplete</code> <p>Decrease health by amount * deplete_rate, clamped to [0, 1].</p> <code>render</code> <p>Render the victim using defined coordinates.</p>"},{"location":"api/sar/#mosaic.sar.objects.VictimBase.can_overlap","title":"can_overlap","text":"<pre><code>can_overlap()\n</code></pre> <p>Victims cannot be walked over.</p>"},{"location":"api/sar/#mosaic.sar.objects.VictimBase.can_pickup","title":"can_pickup","text":"<pre><code>can_pickup()\n</code></pre> <p>Victims can be picked up.</p>"},{"location":"api/sar/#mosaic.sar.objects.VictimBase.deplete","title":"deplete","text":"<pre><code>deplete(amount)\n</code></pre> <p>Decrease health by amount * deplete_rate, clamped to [0, 1]. Negative deplete_rate means the victim regenerates health.</p>"},{"location":"api/sar/#mosaic.sar.objects.VictimBase.render","title":"render","text":"<pre><code>render(img)\n</code></pre> <p>Render the victim using defined coordinates.</p>"},{"location":"api/sar/#mosaic.sar.actions","title":"actions","text":"<p>Classes:</p> Name Description <code>BaseAction</code> <p>Abstract base class for all actions.</p> <code>RescueAction</code> <p>Pick up a real victim (alive/dead) or a fake victim.</p> <code>RescueRewards</code> <p>Reward magnitudes for RescueAction. Neutral plain-RL defaults \u2014</p>"},{"location":"api/sar/#mosaic.sar.actions.BaseAction","title":"BaseAction","text":"<pre><code>BaseAction(env=None, fallback=None)\n</code></pre> <p>Abstract base class for all actions.</p> <p>Methods:</p> Name Description <code>reset</code> <p>Optional per-episode reset hook. No-op by default.</p> <code>verify</code> <p>Check the env's mission after this action. Returns</p>"},{"location":"api/sar/#mosaic.sar.actions.BaseAction.reset","title":"reset","text":"<pre><code>reset(level_gen) -&gt; None\n</code></pre> <p>Optional per-episode reset hook. No-op by default.</p>"},{"location":"api/sar/#mosaic.sar.actions.BaseAction.verify","title":"verify","text":"<pre><code>verify()\n</code></pre> <p>Check the env's mission after this action. Returns (terminated, bonus_reward, info). Default: checks self.env.instrs.verify(). Any action can call this to opt into mission-completion checking; most actions (e.g. movement) shouldn't call it at all.</p>"},{"location":"api/sar/#mosaic.sar.actions.RescueAction","title":"RescueAction","text":"<pre><code>RescueAction(env=None, fallback=None, rewards=None)\n</code></pre> <p>               Bases: <code>BaseAction</code></p> <p>Pick up a real victim (alive/dead) or a fake victim.</p>"},{"location":"api/sar/#mosaic.sar.actions.RescueRewards","title":"RescueRewards  <code>dataclass</code>","text":"<pre><code>RescueRewards(real_victim_alive: float = 1.0, fake_victim: float = -1.0, real_victim_dead: float = -2.0)\n</code></pre> <p>Reward magnitudes for RescueAction. Neutral plain-RL defaults \u2014 override with study-calibrated values via RescueAction(rewards=...).</p>"},{"location":"api/sar/#mosaic.sar.observations","title":"observations","text":"<p>Classes:</p> Name Description <code>GameObservation</code> <p>Processes the raw env observation into an enriched obs dict each step.</p> <code>ObservationProcessor</code> <p>Abstract base class for processing the raw env observation each step.</p> <p>Functions:</p> Name Description <code>cam_bounds</code> <p>Return (x0, y0, x1, y1) camera view bounds from an obs dict.</p> <code>scan_grid</code> <p>Single-pass grid scan returning a (height x width) int list.</p>"},{"location":"api/sar/#mosaic.sar.observations.GameObservation","title":"GameObservation","text":"<p>               Bases: <code>ObservationProcessor</code></p> <p>Processes the raw env observation into an enriched obs dict each step.</p> <p>Instantiated once on the env. Call process_observation(obs, env) each step.</p> <p>Methods:</p> Name Description <code>process_observation</code> <p>Enrich and return the obs dict with full game state.</p>"},{"location":"api/sar/#mosaic.sar.observations.GameObservation.process_observation","title":"process_observation","text":"<pre><code>process_observation(obs: dict, env) -&gt; dict\n</code></pre> <p>Enrich and return the obs dict with full game state.</p>"},{"location":"api/sar/#mosaic.sar.observations.ObservationProcessor","title":"ObservationProcessor","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for processing the raw env observation each step.</p> <p>Methods:</p> Name Description <code>process_observation</code> <p>Enrich and return the obs dict with full game state.</p> <code>reset</code> <p>Optional per-episode reset hook. No-op by default.</p>"},{"location":"api/sar/#mosaic.sar.observations.ObservationProcessor.process_observation","title":"process_observation  <code>abstractmethod</code>","text":"<pre><code>process_observation(obs: dict, env) -&gt; dict\n</code></pre> <p>Enrich and return the obs dict with full game state.</p>"},{"location":"api/sar/#mosaic.sar.observations.ObservationProcessor.reset","title":"reset","text":"<pre><code>reset(level_gen) -&gt; None\n</code></pre> <p>Optional per-episode reset hook. No-op by default.</p>"},{"location":"api/sar/#mosaic.sar.observations.cam_bounds","title":"cam_bounds","text":"<pre><code>cam_bounds(obs: dict) -&gt; tuple[int, int, int, int]\n</code></pre> <p>Return (x0, y0, x1, y1) camera view bounds from an obs dict.</p>"},{"location":"api/sar/#mosaic.sar.observations.scan_grid","title":"scan_grid","text":"<pre><code>scan_grid(env) -&gt; list\n</code></pre> <p>Single-pass grid scan returning a (height x width) int list.</p> Encoding <p>0=empty, 1=wall, 4=lava, 5=victim, 6=fake_victim 10-27: door (DOOR_BASE + color_idx*3 + state; state 0=open,1=closed,2=locked) 30-35: key  (KEY_BASE + color_idx)</p>"},{"location":"api/sar/#mosaic.sar.instructions","title":"instructions","text":"<p>Classes:</p> Name Description <code>PickupAllVictimsInstr</code> <p>Instruction to pick up all victims in the environment.</p>"},{"location":"api/sar/#mosaic.sar.instructions.PickupAllVictimsInstr","title":"PickupAllVictimsInstr","text":"<p>               Bases: <code>Instr</code></p> <p>Instruction to pick up all victims in the environment. This instruction verifies that all victims have been picked up (removed from grid).</p> <p>Methods:</p> Name Description <code>reset_verifier</code> <p>Called on env reset, once this episode's victims are placed; sets</p> <code>surface</code> <p>Return a natural language description of the instruction.</p> <code>verify</code> <p>Verify if all victims have been picked up.</p>"},{"location":"api/sar/#mosaic.sar.instructions.PickupAllVictimsInstr.reset_verifier","title":"reset_verifier","text":"<pre><code>reset_verifier(env)\n</code></pre> <p>Called on env reset, once this episode's victims are placed; sets self.env so verify() can access the grid, and reads this episode's victim count off env.total_victims for num_navs_needed(). Note: surface() can't rely on this \u2014 RoomGridLevel._gen_grid() calls surface() before RoomGridLevel.reset() calls this \u2014 so it reads env.total_victims directly instead.</p>"},{"location":"api/sar/#mosaic.sar.instructions.PickupAllVictimsInstr.surface","title":"surface","text":"<pre><code>surface(env)\n</code></pre> <p>Return a natural language description of the instruction.</p> <p>Parameters:</p> Name Type Description Default <code>env</code> <p>The environment instance</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Description of the instruction</p>"},{"location":"api/sar/#mosaic.sar.instructions.PickupAllVictimsInstr.verify","title":"verify","text":"<pre><code>verify(action)\n</code></pre> <p>Verify if all victims have been picked up.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>'success' if all victims picked up, 'continue' otherwise</p>"},{"location":"api/sar/#mosaic.sar.placers","title":"placers","text":"<p>Classes:</p> Name Description <code>LavaPlacer</code> <p>Handles placement of lava obstacles in the environment.</p> <code>LockedRoomPlacer</code> <p>Handles placement of locked rooms and their corresponding keys.</p> <code>VictimPlacer</code> <p>Places real victims at random unoccupied cells, never in front of a</p>"},{"location":"api/sar/#mosaic.sar.placers.LavaPlacer","title":"LavaPlacer","text":"<pre><code>LavaPlacer(lava_per_room=0, lava_probability=0.3, enabled=True)\n</code></pre> <p>               Bases: <code>Placer</code></p> <p>Handles placement of lava obstacles in the environment.</p> <p>Methods:</p> Name Description <code>place_all</code> <p>Place lava in all rooms based on configuration.</p> <code>place_in_room</code> <p>Place lava tiles at random cells in the room, never adjacent to a door.</p>"},{"location":"api/sar/#mosaic.sar.placers.LavaPlacer.place_all","title":"place_all","text":"<pre><code>place_all(level_gen, num_rows, num_cols, skip_locked_rooms=False)\n</code></pre> <p>Place lava in all rooms based on configuration.</p>"},{"location":"api/sar/#mosaic.sar.placers.LavaPlacer.place_in_room","title":"place_in_room","text":"<pre><code>place_in_room(level_gen, i, j, num_lava=None)\n</code></pre> <p>Place lava tiles at random cells in the room, never adjacent to a door.</p>"},{"location":"api/sar/#mosaic.sar.placers.LockedRoomPlacer","title":"LockedRoomPlacer","text":"<pre><code>LockedRoomPlacer(locked_room_prob=0.35)\n</code></pre> <p>               Bases: <code>Placer</code></p> <p>Handles placement of locked rooms and their corresponding keys.</p>"},{"location":"api/sar/#mosaic.sar.placers.VictimPlacer","title":"VictimPlacer","text":"<pre><code>VictimPlacer(num_real_victims=1)\n</code></pre> <p>               Bases: <code>Placer</code></p> <p>Places real victims at random unoccupied cells, never in front of a door. Victims get VictimBase's neutral defaults (health=1.0, deplete_rate=1.0) and a random direction \u2014 bare minimum, same shape as LockedRoomPlacer. Everything else victim-related \u2014 fake-victim decoys, custom health/decay, targeted placement, spread-out distribution, etc. \u2014 doesn't belong here \u2014 write a separate Placer in experiment/ instead; see experiment/placers.py.</p> <p>Methods:</p> Name Description <code>place_all</code> <p>Place victims at random cells in each room, never in front of a door.</p>"},{"location":"api/sar/#mosaic.sar.placers.VictimPlacer.place_all","title":"place_all","text":"<pre><code>place_all(level_gen, num_rows, num_cols)\n</code></pre> <p>Place victims at random cells in each room, never in front of a door.</p>"}]}