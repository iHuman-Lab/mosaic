# install this package (pulls in core deps declared in pyproject.toml)
-e .

# needed to run human-subject experiments (src/experiment_main.py)
ray
python-dotenv

# ixp is not on PyPI; install from the local sibling repo, e.g.:
#   pip install -e ../ixp
# ixp

# optional: Tobii eye-tracker SDK, only needed for eye-tracking experiments
# tobii-research

# testing
pytest
